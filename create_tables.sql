CREATE TABLE books (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT,
    author TEXT,
    genre TEXT,
    publication_year INTEGER,
    price INTEGER,
    stock INTEGER,
    rating INTEGER,
    language TEXT
);
